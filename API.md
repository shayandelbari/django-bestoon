/submit/expense/
    POST, returns json
    input: date (optional), text, amount, user(token)
    output: status: OK

/submit/income/
    post, returns json
    input: data (optional), text, amount, user(token)
    output: status: OK
